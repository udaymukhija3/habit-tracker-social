# Habit Tracker Social - Environment Variables Template
# Copy this file to .env and update with your actual values

# ============================================
# SECURITY CONFIGURATION (CRITICAL)
# ============================================

# JWT Secret Key - MUST be changed in production
# Generate a secure random key: openssl rand -base64 32
# MINIMUM 256 bits (32 bytes) for HS256 algorithm
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_256_BIT_KEY_IN_PRODUCTION

# JWT Token Expiration (in milliseconds)
# Default: 3600000 (1 hour) - Recommended for production
# Development: Can be longer for convenience
JWT_EXPIRATION=3600000

# ============================================
# DATABASE CONFIGURATION
# ============================================

# MySQL Database
DB_USERNAME=root
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=3306
DB_NAME=habit_tracker

# Full MySQL URL (optional - overrides above if set)
# DB_URL=jdbc:mysql://localhost:3306/habit_tracker?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC

# ============================================
# REDIS CONFIGURATION
# ============================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================
# MONGODB CONFIGURATION
# ============================================

MONGODB_URI=mongodb://localhost:27017/habit_tracker_analytics

# ============================================
# APPLICATION CONFIGURATION
# ============================================

# Server Port
SERVER_PORT=8080

# CORS Configuration
# For development: http://localhost:3000
# For production: https://yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ============================================
# LOGGING CONFIGURATION
# ============================================

# Log Level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# ============================================
# PRODUCTION-SPECIFIC VARIABLES
# ============================================

# Environment (development, staging, production)
SPRING_PROFILES_ACTIVE=development

# Enable/Disable Actuator Endpoints
MANAGEMENT_ENDPOINTS_ENABLED=true

# Enable/Disable Swagger UI (disable in production for security)
SPRINGDOC_SWAGGER_ENABLED=true

# ============================================
# SECURITY NOTES
# ============================================
#
# IMPORTANT: Never commit the actual .env file to version control!
# Add .env to your .gitignore file
#
# To generate a secure JWT secret:
#   openssl rand -base64 32
#
# To generate a secure password:
#   openssl rand -base64 24
#
# For production deployment:
#   - Use AWS Secrets Manager, HashiCorp Vault, or similar
#   - Enable HTTPS/TLS
#   - Use strong passwords (16+ characters)
#   - Rotate secrets regularly
#   - Enable database SSL connections
